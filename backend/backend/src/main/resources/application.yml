spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/production_manager
    username: postgres
    password: 12345

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
