#spring:
#  datasource:
#    url: jdbc:postgresql://postgres:5432/production_manager
#    username: postgres
#    password: 12345
#  jpa:
#    hibernate:
#      ddl-auto: validate
#    show-sql: true
#
#  flyway:
#    enabled: true
#    locations: classpath:db/migration
#    baseline-on-migrate: true

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/production_manager
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
jwt:
  secret: myVeryVeryVeryVeryVeryVerySecretKey1234567890
  expiration: 900000
  refresh-expiration: 900000

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: INFO